# 技能：本地代码阅读器
## 描述
允许 Agent 读取本地项目的源码文件。当用户询问“这段代码是怎么实现的？”或者“帮我看看 main.py 里的逻辑”时，必须调用此工具。

## 约束
1. **安全性**：禁止读取包含 `.env`、`key`、`secret` 等敏感信息的文件。
2. **效率**：如果文件超过 500 行，读取时应告知用户文件过大，并尝试提取关键函数。
3. **格式**：返回内容时，必须指明文件路径，并使用 Markdown 代码块包裹内容。